const events = {
    LOADED_DATA: 'Data loaded from server',
    AFTER_SEARCH: 'After search',
    AFTER_SORT: 'After sort by price',
    AFTER_FILTER: 'After filter categories',
  };
  
  export default events;